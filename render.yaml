services:
  - type: web
    name: commerce-ops-mcp
    runtime: node
    plan: free
    buildCommand: npm install && npm run build
    startCommand: npm run start:http
    envVars:
      - key: NODE_VERSION
        value: 20
